# 需求文档

Spec Type: Feature
Workflow: requirements-first
Status: Requirements Draft
Review Status: unreviewed
Priority: P2  <!-- P0 / P1 / P2 / P3，可省 -->

> 填写约定：不适用的小节**整节删掉**，不要留"暂无 / 待补充"。
> 图示按需用 Mermaid，简单需求不要为图而图。

---

## 一、背景 / 目标 / 范围

**背景与动机**：用 2-4 句话讲清楚**为什么要做**——当前痛点 / 业务诉求是什么。不要写"实现 XX 功能"那种 WHAT 句式。

{{summary}}

**目标**（尽量可衡量）：

- 目标 1：（例：X 操作路径从 5 步缩短至 2 步）
- 目标 2：

**范围之外**（明确不做什么，避免范围蔓延）：

- 本次不覆盖：
- 遗留到下期：

---

## 二、目标用户与场景

- **主要用户**：（例：负责审阅 PR 的资深工程师）
- **典型场景**：（例：每天打开 review queue 一次，希望快速过滤自己作为 author 的条目）
- **非目标用户**：（显式声明谁**不是**目标，避免实现方向飘）

**典型使用路径（Happy Path）**：

1. 用户进入 ____
2. 触发操作 ____
3. 系统响应 ____
4. 完成 ____

> 分支复杂、含循环或决策时，下方可附 Mermaid `flowchart`：
>
> ```mermaid
> flowchart LR
>     A[进入] --> B{校验?}
>     B -->|是| C[响应]
>     B -->|否| D[提示]
>     D --> A
>     C --> E[完成]
> ```

---

## 三、待澄清问题

> 如有需要与用户确认的歧义点，列在下方；无歧义可整节删掉。

- [ ] （待澄清点 1 —— 例：触发入口在哪儿？UI / CLI / 后台定时？）
- [ ] （待澄清点 2）

---

## 四、需求详述

### 需求 1：（一句话标题，描述能力本身，不是验收）

**用户故事**：作为 ____，我希望 ____，以便 ____。

**主流程**（自然语言写清楚正常路径下用户做了 X 看到 Y；写到对方读完不会反问"那如果……怎么办？"为止）：

- 
- 

**异常流程**（输入异常 / 网络异常 / 权限不足等场景下用户看到什么、能怎么继续）：

- 
- 

**验收要点**（可选 — 仅在需要机器可读语义时用 EARS SHALL；上面已说清就略过）：

- 

> 涉及显式状态流转（订单 / 审批 / 工单类）时可附 Mermaid `stateDiagram-v2`：
>
> ```mermaid
> stateDiagram-v2
>     [*] --> 待审核
>     待审核 --> 已通过 : 通过
>     待审核 --> 已驳回 : 驳回
>     已驳回 --> 待审核 : 修改重提
>     已通过 --> [*]
> ```

---

### 需求 2：……

（按上面格式重复；每条需求都是一段可独立讨论、可独立验收的能力）

---

## 五、非功能 / 约束（可选）

- 性能：（如有具体 SLA 写这里）
- 兼容：（版本 / 渠道 / 设备）
- 安全 / 权限：
- 与既有功能的关系：（不要破坏什么）

## 六、依赖与风险（可选）

- 上下游依赖：（哪些外部服务 / 团队需要确认？）
- 主要风险与应对：（最坏情况是什么？怎么兜底？）
- 回滚方案：（出问题如何回退？功能开关 / 灰度 / 数据回滚）

## 七、UI 交互细节（可选，仅 UI 类需求）

- 加载态 / 空态 / 错误态 / 权限隔离 / 响应式：每项一句话，不适用就删行
- 关键文案：（按钮、提示、错误信息中容易反复挑剔的）
- 设计稿链接：
